package com.aerochaser.domain.usecase

import org.junit.Assert.assertEquals
import org.junit.Test

class ScanDirectoryUseCaseTest {
    @Test
    fun `scan directory returns correct imported count`() {
        // Dummy test for v0.15
        assertEquals(4, 2 + 2)
    }
}
